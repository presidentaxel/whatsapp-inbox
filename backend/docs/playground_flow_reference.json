{
  "$comment": "Référence alignée sur flow_runtime_service.py (try_run_playground_flow), playground_flow_service.py, flowMigrate.js, flowNodes.jsx. Vérifier ui_backend_mismatches après toute évolution du moteur.",
  "ui_backend_mismatches": {
    "description": "L’UI dessine des branches ou comportements que le moteur playground n’implémente pas - risque de scénarios qui semblent corrects sur le canevas mais ne se comportent pas ainsi en production.",
    "items": [
      {
        "id": "timeWindowNode",
        "title": "Routage horaire (timeWindowNode)",
        "ui": "Poignées source inside / outside (pendant vs hors plage).",
        "backend": "_successor(edges, cursor) sans handle - premier successeur ou arête sans sourceHandle ; branches inside/outside non distinguées (bloc waitUntilNode/timeWindowNode/logicNode hors mode « si » : passthrough, voir flow_runtime_service.py ~686-691).",
        "impact": "Ex. message « bureaux fermés » sur branche outside ne s’exécute pas à 2h : le flux suit un seul chemin comme si les deux branches étaient équivalentes."
      },
      {
        "id": "waitUntilNode",
        "title": "Attente calendaire (waitUntilNode)",
        "ui": "Champ date / intention « attendre jusqu’à ».",
        "backend": "Passthrough immédiat - aucune attente calendaire serveur (même branche que timeWindowNode).",
        "impact": "Un nœud « jusqu’au 1er janvier » n’attend pas : l’enchaînement continue tout de suite. Seul delayNode applique une vraie pause (durée relative en s/m/h/d, voir delayScheduling)."
      },
      {
        "id": "logicNode_et_ou",
        "title": "Logique ET / OU (logicNode)",
        "ui": "Modes logicMode « ou » et « et » avec poignées multiples.",
        "backend": "Pour logicMode autre que « si » : passthrough simple (_successor sans routage multi-sortie).",
        "impact": "Les modes ET/OU ne routent pas : pas de filtrage ni de multi-sortie réelle."
      },
      {
        "id": "logicNode_si",
        "title": "Logique « si » : condition non évaluée",
        "ui": "Champ condition + branches true / false.",
        "backend": "Aucune évaluation de condition ; cursor = _successor(..., \"true\") ou _successor(...) (poignée true prioritaire, sinon arête sans sourceHandle ou première arête). La poignée « false » n’est pas sélectionnée explicitement par le moteur.",
        "impact": "Ce n’est pas un IF métier : le chemin ne dépend pas du texte ou des variables. Pour un routage basé sur le contenu, utiliser routerNode, interactiveNode ou gemini."
      }
    ]
  },
  "product_gaps_vs_market": {
    "description": "Fonctionnalités souvent présentes dans les outils type Make / Chatfuel / ManyChat, absentes ou non couvertes par des nœuds playground dédiés aujourd’hui.",
    "items": [
      {
        "id": "http_request",
        "title": "Nœud appel HTTP / webhook sortant",
        "gap": "Pas de requête API depuis le graphe (ex. CRM, Airtable, Google Sheet) pour enrichir le contexte avec bot_flow_state.phoneNumber ou d’autres variables.",
        "impact": "Impossible d’inférer côté bot si un numéro est déjà client sans poser la question à l’utilisateur ou traiter hors-flux."
      },
      {
        "id": "set_variable",
        "title": "Nœud « Set variable »",
        "gap": "Persistance variables surtout via interactiveNode (saveToVariable / saveValue sur un choix), saisie utilisateur (varKey), gemini (varKey + intention). Pas de nœud pour forcer une variable en cours de parcours (ex. score_lead = \"chaud\").",
        "impact": "Pas de déduction ou assignation arbitraire déclenchée par le scénario seul."
      },
      {
        "id": "tags_without_handoff",
        "title": "Tags conversation sans handoff",
        "gap": "Champ tagsText présent sur handoffNode dans l’UI du graphe ; le runtime handoff (flow_runtime_service) appelle set_conversation_bot_mode(False) et _escalate_to_human avec internalMessage / message entrant - tagsText n’est pas lu côté serveur playground. Effet produit : toute intention « taguer pour le suivi » reste liée au bloc Handoff qui coupe le bot.",
        "impact": "Pas de chemin documenté pour taguer « Lead froid » tout en laissant le bot actif ; en plus, la persistance des tags depuis le graphe n’est pas implémentée dans le moteur actuel."
      },
      {
        "id": "outbound_media",
        "title": "Envoi médias natifs (image, PDF, audio)",
        "gap": "Nœuds documentés ici : sendText, sendTemplate uniquement. Pas de nœud sendImage/sendDocument/sendAudio dans le playground.",
        "impact": "Médias hors gabarits Meta approuvés : pas d’envoi direct via un bloc dédié (les templates peuvent embarquer des médias si le modèle le prévoit)."
      }
    ]
  },
  "graph": {
    "description": "Champ graph en base (playground_flows) ou published_playground_flow (legacy).",
    "shape": {
      "v": 2,
      "nodes": [],
      "edges": []
    },
    "normalization_backend": {
      "source": "playground_flow_service._normalize_graph",
      "rules": "Force nodes et edges en listes ; v défaut 2."
    }
  },
  "reactFlowNode": {
    "id": "string, unique dans le graphe",
    "type": "voir nodeTypes",
    "position": { "x": "number", "y": "number" },
    "data": "objet selon le type ; varKey complété par migrateNode si absent"
  },
  "reactFlowEdge": {
    "id": "string recommandé",
    "source": "id nœud source",
    "target": "id nœud cible",
    "sourceHandle": "string | null - ids des poignées source pour le routage",
    "targetHandle": "optionnel"
  },
  "variableSubstitution": {
    "syntax": "{{nomVariable}}",
    "source": "_subst_vars",
    "storage": "conversations.bot_flow_state.variables",
    "builtin_each_turn": {
      "description": "Renseignés au début de chaque exécution du flux depuis le contact / la conversation (écrasent les clés du même nom dans variables).",
      "keys": {
        "contact_name": "display_name ou whatsapp_number du contact",
        "nom_client": "alias de contact_name",
        "contact_phone": "whatsapp_number ou client_number de la conversation",
        "numero_client": "alias de contact_phone",
        "contact_first_name": "premier mot du display_name, sinon contact_name",
        "prenom_client": "alias de contact_first_name",
        "contact.firstName": "alias de contact_first_name (notation objet)",
        "contact.first_name": "alias de contact_first_name",
        "contact.name": "alias de contact_name",
        "contact.phone": "alias de contact_phone"
      }
    }
  },
  "bot_flow_state": {
    "description": "Session JSON sur conversations.bot_flow_state",
    "fields": {
      "phoneNumber": "string",
      "currentNodeId": "string | null - en attente réponse interactiveNode",
      "lastInteractionAt": "ISO UTC",
      "wabaOptIn": "boolean",
      "variables": "dict",
      "continueFromNodeId": "string | null - reprise du parcours",
      "afterInteractiveTarget": "string | null",
      "entryStartNodeId": "string | null - start choisi pour la session",
      "activeFlowId": "string | legacy - id flux résolu ou mot legacy",
      "flowDelayUntil": "ISO UTC | null - réveil après delayNode ou timeout interactif",
      "flowDelayResumeNodeId": "string | null - nœud à exécuter après le délai (delayNode ou branche timeout)"
    },
    "reset_when_flow_changes": "Si activeFlowId change : variables, currentNodeId, continueFromNodeId, afterInteractiveTarget, entryStartNodeId, flowDelayUntil, flowDelayResumeNodeId sont remis à zéro."
  },
  "delayScheduling": {
    "description": "Nœud delayNode : attente réelle, pas un passthrough.",
    "implementation": "Boucle asyncio periodic_playground_flow_delays (~45s) + fetch_due_playground_delay_conversation_ids ; pas Celery.",
    "maxDelaySeconds": 2592000,
    "maxDelayNote": "30 jours - _FLOW_DELAY_MAX_SECONDS",
    "onDelayNode": [
      "Calcule secondes depuis duration (nombre > 0) et unit s|m|h|d",
      "Si durée invalide ou ≤ 0 : enchaîne comme avant (successeur immédiat)",
      "Sinon : flowDelayUntil = maintenant + delta, flowDelayResumeNodeId = successeur, vide currentNodeId / continueFromNodeId / afterInteractiveTarget, persiste, return True"
    ],
    "whileDelayPending": "Message entrant : si maintenant < flowDelayUntil et pas d’attente interactive (currentNodeId vide), persiste et return True. Si currentNodeId est défini (timeout + réponse attendue), le message est traité normalement.",
    "whenDelayExpires": [
      "Soit le prochain message client : _consume_due_flow_delay → continueFromNodeId = resume (currentNodeId / afterInteractiveTarget vidés si présents)",
      "Soit la tâche périodique : try_run_playground_flow(..., scheduled_delay_wake=true)"
    ],
    "interactiveTimeout": "Après envoi interactiveNode : si arête timeout → flowDelayUntil = now + durée, flowDelayResumeNodeId = cible. Réponse utilisateur sur ce nœud : flowDelayUntil et flowDelayResumeNodeId remis à null.",
    "multiInstanceWarning": "Plusieurs workers API peuvent tenter le réveil ; le premier consomme flowDelayUntil."
  },
  "nodeTypes": {
    "start": {
      "data": {
        "varKey": "string",
        "triggerType": ["message_in", "schedule", "webhook", "manual", "playground_audience"],
        "messageMatch": ["any", "contains", "equals", "regex"],
        "messageKeyword": "string",
        "scheduleAt": "string",
        "scheduleRepeat": ["none", "daily", "weekly", "monthly"],
        "webhookSecretRef": "string",
        "audienceBroadcastGroupId": "string - groupe de diffusion (persisté) pour type playground_audience",
        "campaignScheduledFor": "string - datetime-local brouillon date/heure de lancement (persisté)",
        "entryPriority": "number - priorité décroissante entre starts qui matchent",
        "playground_audience_note": "Pas de message sur ce nœud : à l’heure planifiée le moteur enchaîne depuis ce start vers le successeur (organigramme)."
      },
      "handles": { "source": [null], "target": [] }
    },
    "sendText": {
      "data": { "body": "string", "varKey": "string" },
      "handles": { "target": [null], "source": [null] },
      "outbound": "Message texte WhatsApp uniquement - pas de pièce jointe via ce nœud."
    },
    "sendTemplate": {
      "data": {
        "selectedTemplateKey": "name||lang",
        "templateName": "string",
        "templateLanguage": "string",
        "variableValues": "dict",
        "quickReplyButtons": "array",
        "varKey": "string"
      },
      "handles": { "target": [null], "source": [null] },
      "outbound": "Template WhatsApp approuvé ; médias éventuels selon le modèle Meta, pas un upload arbitraire depuis le playground."
    },
    "interactiveNode": {
      "data": {
        "body": "string",
        "uiKind": ["buttons", "list"],
        "choices": [{ "id": "string", "title": "string", "description": "string", "saveToVariable": "string", "saveValue": "any" }],
        "listButtonText": "string",
        "timeoutDuration": "number optionnel (ex. 48)",
        "timeoutUnit": "string optionnel s|m|h|d - défaut h",
        "varKey": "string"
      },
      "handles": { "target": [null], "source": [null, "timeout"] },
      "limits_runtime": { "buttons": "max 3, titre 20 car.", "list": "max 10 lignes, titre 24, description 72" },
      "timeout_runtime": "Si arête sourceHandle=timeout et timeoutDuration>0 : flowDelayUntil / flowDelayResumeNodeId ; réponse utilisateur annule le délai"
    },
    "routerNode": {
      "data": { "routes": [{ "label": "string", "match": "string" }], "varKey": "string" },
      "handles": { "target": [null], "source": ["route-0", "route-1", "...", "escape"] }
    },
    "gemini": {
      "data": {
        "hint": "string",
        "systemPrompt": "string",
        "intents": [{ "keyword": "string", "label": "string" }],
        "varKey": "string"
      },
      "handles": {
        "sans_intentions": { "source": [null] },
        "avec_intentions": { "source": ["intent-0", "intent-1", "...", "intent-unknown"] }
      }
    },
    "handoffNode": {
      "description": "Type UI ; backend accepte aussi l’alias legacy \"handoff\".",
      "data": {
        "tagsText": "string - présent dans le graphe UI ; non consommé par flow_runtime_service au moment de la doc",
        "assignAgent": "string",
        "internalMessage": "string - utilisé comme note si non vide, sinon message entrant",
        "varKey": "string"
      },
      "handles": { "target": [null], "source": [null] },
      "runtime": "set_conversation_bot_mode(False), _escalate_to_human(conversation, inbound_text ou internalMessage), continueFromNodeId = successeur optionnel ; pas de lecture de tagsText ni assignAgent dans le moteur playground documenté"
    },
    "delayNode": {
      "data": {
        "duration": "string ou nombre (interprété en float)",
        "unit": ["s", "m", "h", "d"],
        "varKey": "string"
      },
      "handles": { "target": [null], "source": [null] },
      "runtime": "Voir section delayScheduling - attente réelle jusqu’à flowDelayUntil puis reprise."
    },
    "waitUntilNode": {
      "data": { "until": "string", "timezoneNote": "string", "varKey": "string" },
      "handles": { "target": [null], "source": [null] },
      "runtime": "passthrough immédiat (pas d’attente calendaire côté serveur) - voir ui_backend_mismatches.waitUntilNode",
      "workaround": "Utiliser delayNode pour une attente réelle relative (max 30 jours)."
    },
    "timeWindowNode": {
      "data": {
        "activeDays": "array de strings",
        "startTime": "HH:MM",
        "endTime": "HH:MM",
        "varKey": "string"
      },
      "handles_UI": { "source": ["inside", "outside"] },
      "runtime": "passthrough : _successor sans handle - branches inside/outside non distinguées par le moteur - voir ui_backend_mismatches.timeWindowNode"
    },
    "logicNode": {
      "data": { "logicMode": ["si", "ou", "et"], "condition": "string", "varKey": "string" },
      "handles_UI": {
        "si": { "source": ["true", "false"] },
        "ou": { "source": ["a", "b"] },
        "et": { "target": ["inA", "inB"], "source": [null] }
      },
      "runtime": {
        "si": "Non conditionnel : data.condition ignoré ; _successor(..., \"true\") puis repli _successor(...) - voir ui_backend_mismatches.logicNode_si",
        "ou_et": "passthrough simple - pas de routage multi-sortie - voir ui_backend_mismatches.logicNode_et_ou"
      }
    }
  },
  "migrations_UI": {
    "source": "frontend flowMigrate.js",
    "legacyTypes": { "ifNode": "logicNode logicMode si", "orNode": "logicNode logicMode ou" }
  },
  "inboundSignals": {
    "source": "extract_inbound_flow_signals",
    "fields": ["text", "button_id", "list_row_id"]
  },
  "limits": {
    "maxStepsPerInvocation": 40
  }
}
