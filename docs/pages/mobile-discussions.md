# Page Mobile Discussions

- Route: `/discussions`.
- Container: `MobileInboxPage` (mode `conversations`).
- Composant: `MobileConversationsList`.
- APIs: `getAccounts`, `getConversations`, `markConversationRead`.
- Verification:
  - badge unread correct;
  - pagination `onLoadMore` fonctionnelle.
