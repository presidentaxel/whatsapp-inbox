# Page Mobile Equipe

- Route: `/equipe`.
- Container: `MobileInboxPage` (mode `team`).
- Composant: `MobileTeamPanel`.
- Verification:
  - navigation onglet equipe active;
  - retour vers discussions fonctionnel.
