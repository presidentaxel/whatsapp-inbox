# Page Mobile Parametres Compte

- Route: `/parametres/compte`.
- Container: `MobileInboxPage`.
- Composant: `MobileAccountSettings`.
- Verification:
  - affichage page compte;
  - retour vers `/parametres` fonctionnel.
